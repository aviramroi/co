# Wingman web environment

# URL of the Wingman agent's control API (server-side only; never exposed to the
# browser — all agent calls are proxied through this app's /api/agent BFF).
WINGMAN_API=http://127.0.0.1:4600
